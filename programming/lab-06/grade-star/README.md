# Судоку

Проверка готового поля 9×9: цифры 1–9 должны встретиться ровно по одному разу в каждой строке, столбце и каждом квадрате 3×3.

В [sudoku.c](sudoku.c) это три отдельные проверки. Есть пример правильного судоку и поля с ошибкой.

`gcc sudoku.c -o sudoku && ./sudoku`
