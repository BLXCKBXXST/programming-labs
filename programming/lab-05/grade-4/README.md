# Комплексные числа

Числа хранятся в двух массивах, `re[]` и `im[]`. Программа заполняет их случайными значениями, считает энергию сигнала как сумму `re² + im²` для всех элементов и находит максимальную действительную часть.

[Решение](zadanie1.c), запуск: `gcc zadanie1.c -o grade4 && ./grade4`.
