# Символы и числа

Одно и то же значение можно показать как символ (`%c`) или код (`%d`). Например, ASCII-код 63 — это `?`, а 99 — `c`.

[chars.c](chars.c) выводит:

```text
? == 63
r == 114
b == c
```

Сборка обычная: `gcc chars.c -o chars`.
