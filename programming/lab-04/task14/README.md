# Сколько единиц в двоичной записи

В [2zadanie.c](2zadanie.c) использован приём `a &= (a - 1)`: на каждом шаге исчезает одна единица. Остаётся посчитать, сколько раз это получилось сделать до нуля.

У числа 13 (`1101₂`) три единицы, у 255 — восемь.

`gcc 2zadanie.c -o task14 && ./task14`
