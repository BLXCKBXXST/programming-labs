# Лаба 9 — Git

Пузырьковая сортировка в [sort.c](sort.c), а текстовые файлы рядом нужны для упражнений с Git. [Скриншоты](screenshots.md) вынесены отдельно, чтобы не листать их каждый раз при открытии README.

Сборка сортировки: `gcc sort.c -o sort && ./sort`.
