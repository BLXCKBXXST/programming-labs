# Лаба 15.2 — ещё отладка

Задания из [debug_task](https://github.com/kruffka/C-Programming/tree/debug_task). Первые четыре повторяют ошибки из [лабы 11](../lab-11/), так что подробный разбор там. На 5 добавлено задание с шифрованием, отдельно — симулятор 4G/5G.

Исходники — в [src](src), заметки по дополнительному заданию — в [DEBUG_LOG.md](DEBUG_LOG.md). Makefile включает ASan и UBSan.

`make all` для сборки, затем запускай нужный `./src/task1` … `./src/task_star`.
