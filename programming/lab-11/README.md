# Лаба 11 — найти ошибки

Тут четыре намеренно сломанных программы. Для каждой есть `before.c` и исправленная `after.c`.

[Указатели и segfault](task-1/), [переполнение буфера](task-2/), [ошибка в макросе](task-3/), [неправильная транспозиция](task-4/). Подробности оставлены возле каждого задания.

Обычный `make` собирает только исправленные версии. Исходники с ошибками лучше проверять отдельно, при необходимости с AddressSanitizer.

[Методичка](Лабораторная%20работа%20№11.pdf).
