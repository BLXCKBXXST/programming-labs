# Макрос без скобок

`#define SQR(x) x * x` ломается на выражениях. Если `y = 5`, то `SQR(y + 1)` превращается в `y + 1 * y + 1` и даёт 11 вместо 36.

В [after.c](after.c) макрос исправлен на `((x) * (x))`. [before.c](before.c) — исходный пример ошибки.

`gcc after.c -o after && ./after`
