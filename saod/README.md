# Структуры и алгоритмы обработки данных

Учебный проект на C: лабораторные работы по курсу «Структуры и алгоритмы обработки данных».
Каждая программа реализует один или несколько алгоритмов, выводит результаты в виде таблиц
и сравнивает фактические значения трудоёмкости (M — пересылки, C — сравнения)
с теоретическими оценками из методички.

## Содержание

- [Структура каталога](#структура-каталога)
- [Сборка и запуск](#сборка-и-запуск)
- [Список работ](#список-работ)
- [Теоретическая часть](#теоретическая-часть)
- [Документация](#документация)

## Структура каталога

```
.
├── README.md                  ← этот файл
├── Makefile                   ← сборка
├── .gitignore
├── docs/                      ← методички и теоретическая таблица
│   ├── arrays.pdf             ← задания по массивам (1.1–1.10)
│   ├── lists.pdf              ← задания по спискам и хешированию (2.1–2.5)
│   └── labor_intensity.pdf    ← теоретические оценки M и C
├── lib/                       ← общие модули
│   ├── sorting.{c,h}          ← алгоритмы сортировки массивов
│   ├── list.{c,h}             ← список / стек / очередь / MergeSort / DigitalSort
│   ├── search.{c,h}           ← двоичный поиск
│   └── hash.{c,h}             ← хеширование (chaining + open addressing)
├── src/
│   ├── arrays/                ← задачи 1.1–1.10
│   │   ├── README.md
│   │   └── 1_*.c
│   └── lists/                 ← задачи 2.1–2.5
│       ├── README.md
│       └── 2_*.c
└── build/                     ← результаты сборки (создаётся автоматически)
    └── bin/                   ← бинарники
```

## Сборка и запуск

### Требования
- `gcc` с поддержкой C99 (или новее)
- GNU Make
- Стандартная `libm` (математика)

### Сборка
```sh
make              # собрать все 15 программ
make <target>     # собрать одну программу (см. список ниже)
make clean        # удалить build/ и project.pack
make help         # показать встроенную справку
```

Бинарники появляются в `build/bin/`.

### Запуск
```sh
./build/bin/1_1_select_sort     # программа задачи 1.1
./build/bin/2_2_merge_sort      # программа задачи 2.2
./build/bin/2_4_hash_chain      # программа задачи 2.4
```
Программы не требуют параметров и сами выводят все таблицы.

Для удобства Makefile принимает и короткие алиасы: `make select`, `make merge`,
`make hash_chain` — собирают тот же бинарник, что и `make 1_1_select_sort`,
`make 2_2_merge_sort`, `make 2_4_hash_chain` соответственно.

### Сериализация исходников
Для удобной передачи проекта одним файлом:
```sh
make pack         # упаковать все .c/.h/Makefile/README в project.pack
make unpack       # распаковать обратно
```

## Список работ

### Часть 1 — массивы

| #    | Тема                                      | Бинарник                   | Алиас     |
|------|-------------------------------------------|----------------------------|-----------|
| 1.1  | Сортировка прямого выбора (SelectSort)    | `1_1_select_sort`          | `select`  |
| 1.2  | Пузырьковая сортировка (BubbleSort)       | `1_2_bubble_sort`          | `bubble`  |
| 1.3  | Шейкерная сортировка (ShakerSort)         | `1_3_shaker_sort`          | `shaker`  |
| 1.4  | Сортировка прямого включения (InsertSort) | `1_4_insert_sort`          | `insert`  |
| 1.5  | Сортировка Шелла (ShellSort)              | `1_5_shell_sort`           | `shell`   |
| 1.6  | Двоичный поиск (BSearch)                  | `1_6_binary_search`        | `bsearch` |
| 1.7  | Сортировка массивов структур              | `1_7_struct_sort`          | `struct`  |
| 1.8  | Индексация массивов                       | `1_8_index_sort`           | `index`   |
| 1.9  | Пирамидальная сортировка (HeapSort)       | `1_9_heap_sort`            | `heap`    |
| 1.10 | Быстрая сортировка (QuickSort)            | `1_10_quick_sort`          | `quick`   |

Подробности — в [src/arrays/README.md](src/arrays/README.md).

### Часть 2 — списки и хеширование

| #   | Тема                                              | Бинарник            | Алиас        |
|-----|---------------------------------------------------|---------------------|--------------|
| 2.1 | Стек, очередь, операции со списком                | `2_1_list`          | `list`       |
| 2.2 | Сортировка прямого слияния (MergeSort)            | `2_2_merge_sort`    | `merge`      |
| 2.3 | Цифровая сортировка (DigitalSort, LSD-radix)      | `2_3_digital_sort`  | `digital`    |
| 2.4 | Хеширование методом прямого связывания (chaining) | `2_4_hash_chain`    | `hash_chain` |
| 2.5 | Хеширование методом открытой адресации            | `2_5_hash_open`     | `hash_open`  |

Подробности — в [src/lists/README.md](src/lists/README.md).

## Теоретическая часть

Все теоретические оценки M и C взяты из таблицы [docs/labor_intensity.pdf](docs/labor_intensity.pdf).
Каждая программа выводит:

1. **Главную таблицу ТЗ** — по форме методички. Колонка «M+C теоретич.»
   расширена до трёх (Убыв./Случ./Возр.), если теория зависит от типа массива
   (Bubble, Shaker, Insert, Quick); одна колонка, если теория одинакова для всех
   случаев (Select, Heap, Merge, Digital).
2. **Раздел `=== Доп. ===`** — детальный разбор: теория `Mt`, `Ct` отдельно vs
   фактические `Мф`, `Сф` по типам массивов.

На убывающих и возрастающих массивах фактические значения **точно совпадают**
с теорией; на случайных — близки к среднему случаю (отклонение в пределах
нескольких процентов).

Реализации алгоритмов в `lib/` следуют **псевдокодам методички**
[docs/methodical manual.pdf](docs/methodical%20manual.pdf) буквально: те же
имена переменных (`i, j, k, t, L, R, x, h, d, p`), 1-индексация
(`for (i=1; i<=n; i++) ... arr[i-1] ...`), та же структура циклов. Каждая
функция обрамлена коробочными маркерами `НАЧАЛО:/КОНЕЦ:`, что позволяет
копировать фрагмент кода в отчёт без захвата соседних функций.

## Документация

- [src/arrays/README.md](src/arrays/README.md) — карта задач 1.1–1.10.
- [src/lists/README.md](src/lists/README.md) — карта задач 2.1–2.5.
- [docs/methodical manual.pdf](docs/methodical%20manual.pdf) — методическое
  пособие СибГУТИ (Курапова Е.В., Мачикина Е.П., 2006); 105 стр., псевдокоды всех алгоритмов.
- [docs/arrays.pdf](docs/arrays.pdf), [docs/lists.pdf](docs/lists.pdf) — оригинальные ТЗ.
- [docs/labor_intensity.pdf](docs/labor_intensity.pdf) — таблица теоретических оценок M и C.
