# Лабораторная работа №15: Текстовые и бинарные файлы.

Предметная область: (Имя, ID, Уровень).

## Структура

```
lab-15/
├── grade-3/   # чтение CSV, вывод таблицы
├── grade-4/   # + бинарный файл (fwrite/fread), поиск по ID
├── grade-5/   # + RLE-сжатие и декомпрессия
└── README.md
```

| Grade | Ключевые темы |
|-------|---------------|
| 3     | `fgets`, `strtok`, форматированный вывод |
| 4     | `fwrite`, `fread`, бинарный файл, поиск |
| 5     | RLE-сжатие, `compress()`, `decompress()` |
